# Frontend

Vite + React app.

## Commands

```sh
pnpm install
pnpm run dev
pnpm run build:prod
pnpm run lint
```

## Env

- `.env.dev` is committed and used by `pnpm run dev`.
- `.env.prod` is committed and used by `pnpm run build:prod`.
